# 에이전트 작업 규칙

이 문서는 모델과 도구에 관계없이 적용하는 저장소 작업 규약입니다.
이 파일을 자동으로 읽지 않는 도구도 작업 전에 직접 읽어야 합니다.

## 범위

- 이 저장소는 RPG 게임의 기획만 담당합니다. 게임 코드, 엔진 설정, 렌더링 및 자산 제작은 범위 밖입니다.
- 엔진, 아트스타일, 세부 구현은 미정입니다. 장르나 프로젝트 이름에서 구체적인 게임 규칙을 추정하지 않습니다.
- 특정 AI 모델, 서비스의 메모리, 전용 파일 형식에 의존하지 않습니다.
- 기획은 한국어로 기록합니다. 파일명과 ID는 검색·이동에 편리한 ASCII를 사용합니다.
- 저장소 관리와 기획 검증을 위한 작은 도구는 둘 수 있지만 게임 구현과 혼동하지 않습니다.

## 세션 시작

1. `README.md`, `planning/project.md`, `index/README.md`를 읽습니다.
2. `index/catalog.json`에서 관련 ID, 제목, 요약, 태그를 찾습니다.
3. 해당 파일과 `source_ids`, `related_ids`의 문서를 읽습니다. 요약만으로 판단하지 않습니다.
4. `planning/questions.md`와 관련 문서의 미해결 질문을 확인합니다.
5. 파일과 Git 상태를 확인해 사용자의 기존 변경 위에서 작업합니다.

색인 검색에 없는 내용은 `planning/` 전체도 검색합니다. 미등록 파일을 발견하면 읽고 색인을 보완합니다.
세션의 기억보다 저장된 원문과 최신 사용자 지시를 우선합니다. 서로 충돌하면 충돌을 명시합니다.

## 아이디어를 받았을 때

1. 사용자 입력을 `planning/inbox/`의 새 원문 파일에 보존하고 `source`로 색인합니다. 핵심 표현을 바꾸지 않습니다.
2. 기존 기획을 검색합니다. 기존 개념의 보완이면 해당 문서를 갱신하고, 독립된 개념이면 문서를 나눕니다.
3. 사용자의 확정 지시와 탐색 중인 아이디어를 구분합니다. 명시적인 결정은 `confirmed`, 단순 제안은 `proposed`, 에이전트가 작성 중인 안은 `draft`로 둡니다.
4. 의미가 불분명한 부분은 미정으로 남깁니다. 저장할 수 있는 내용부터 기록하고, 이후 기획에 필요한 질문만 사용자에게 확인합니다.
5. 변경 문서와 같은 작업에서 색인의 경로, 상태, 요약, 태그, 출처, 관계, 갱신일을 수정합니다.
6. 무결성 검사 후 사용자에게 저장 위치와 핵심 해석, 남은 질문을 간결하게 알립니다.

사용자에게 서식을 요구하지 않습니다. 자유로운 발화를 파일로 정리하는 것은 에이전트의 역할입니다.
에이전트의 보완 제안은 제안임을 문서에 밝히며, 사용자 의도로 확정하지 않습니다.
상태의 의미와 허용 조합은 `index/README.md`를 따릅니다.

## 파일과 참조

- 내용의 원본은 기획 파일, 검색용 메타데이터의 원본은 `index/catalog.json`입니다.
- 기획 파일은 `planning/` 아래에 두고 모두 색인합니다. 각 폴더의 `README.md`는 탐색 안내만 담으며 색인 대상에서 제외합니다.
- 문서별 고유 ID를 발급하고 영구적으로 유지합니다. 파일을 이동하거나 제목을 바꿔도 ID를 바꾸지 않습니다.
- 파일 경로는 저장소 루트 기준 상대 경로와 `/`를 사용합니다. 본문의 Markdown 링크는 해당 문서 기준 상대 경로를 사용합니다.
- 중요한 의존·충돌·대체 관계는 본문에서 의미를 설명하고 `related_ids`에도 등록합니다. 출처 원문은 `source_ids`에 등록합니다.
- 같은 규칙과 수치를 여러 곳에 복제하지 않습니다. 대표 파일을 지정하고 다른 문서는 ID와 링크로 참조합니다.
- 한 파일에는 하나의 일관된 주제나 데이터셋을 담습니다. 파일 분할은 실제 내용과 탐색 필요에 따라 결정합니다.

## 형식 선택

- MD: 의도, 경험, 규칙의 설명, 근거, 예외, 열린 질문.
- JSON: 중첩된 구조나 명시적인 자료형이 필요한 데이터. 주석은 넣지 않습니다.
- CSV: 반복되는 동일 구조의 행. UTF-8, 헤더, 안정적인 행 ID를 사용합니다.
- JSON·CSV 데이터에는 열·필드의 의미, 단위, 허용값, 누락값의 의미, 고유 키를 설명하는 MD 문서를 연결합니다.
- 미정 숫자에 임의의 기본값이나 `0`을 넣지 않습니다. JSON의 `null`, CSV의 빈 칸 등을 쓰되 연결 문서에서 의미를 정합니다.
- 특정 엔진의 클래스, 에셋 경로, 프레임 시간, 플랫폼 API를 기획 규칙의 전제로 삼지 않습니다. 플레이어에게 보이는 조건과 결과를 기술합니다.

## 변경과 충돌

- 확정 기획과 충돌하는 새 발언을 받으면 출처와 충돌을 기록합니다. 사용자가 대체를 명확히 지시한 경우에는 바로 반영합니다.
- 대체 지시가 불명확하면 기존 확정안을 유지하고 새 안을 `proposed`로 기록합니다.
- 중요한 선택에는 결정 문서를 남겨 배경, 선택, 근거, 영향, 대체되는 ID를 설명합니다.
- 대체 문서는 `superseded`, 보관 문서는 `archived`로 바꾸고 색인을 유지합니다. 참조된 ID를 삭제하거나 재사용하지 않습니다.
- 원문은 수정해서 과거 의도를 바꾸지 않습니다. 정정은 새 원문을 만들고 관계를 연결합니다.
- Git 기록은 변경 이력을 보조하지만, 현행 결정과 이유는 현재 파일만으로도 이해할 수 있게 남깁니다.

## 작업 종료

- `python tools/check_integrity.py`를 실행합니다. 실행할 수 없으면 그 사실과 수동으로 확인한 범위를 보고합니다.
- 변경 기획의 정의, 조건, 결과, 예외, 단위, 참조와 기존 확정안 사이의 모순을 검토합니다.
- 해결된 질문은 결과와 근거를 남기고 정리합니다. 아직 결정되지 않은 내용은 결정된 것처럼 기록하지 않습니다.
- 검사 성공은 구조적 무결성만 의미합니다. 의미상 모순이 없다는 증거로 사용하지 않습니다.
- 이번 작업의 요청에 커밋이나 게시가 포함되지 않았다면 자동으로 커밋·푸시하지 않습니다.
